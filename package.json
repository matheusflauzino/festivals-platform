{
  "name": "fenac-platform",
  "private": true,
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "turbo": "^2.10.13"
  }
}
